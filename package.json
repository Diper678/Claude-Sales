{
  "name": "{{PROJECT_NAME_SLUG}}",
  "version": "1.0.0",
  "private": true,
  "description": "Sisteco project — {{PROJECT_NAME}}",
  "scripts": {
    "start": "npx serve . -l 3000",
    "init": "node scripts/init-project.js"
  }
}
